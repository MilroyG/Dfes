/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Mar 17 2025 | 17:11:27 */
document.addEventListener("DOMContentLoaded", function () {
    if (!document.getElementById("dmrpmap")) {
        console.error("Map container not found.");
        return;
    }

    var map = L.map("dmrpmap").setView([15.4271947,73.9293256], 10.5);
// 	([15.5, 73.2], 8)

    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
        attribution: "© OpenStreetMap contributors",
    }).addTo(map);

    function dmsToDecimal(dms) {
        dms = dms.replace(/�/g, "°").replace(/˚/g, "°");

        let regex = /(\d+)°\s*(\d+)'?\s*(\d+(?:\.\d+)?)"?\s*([NSEW])/;
        let match = dms.match(regex);

        if (!match) {
            console.error("Invalid DMS format:", dms);
            return null;
        }

        let degrees = parseFloat(match[1]);
        let minutes = parseFloat(match[2]);
        let seconds = parseFloat(match[3]);
        let direction = match[4];

        let decimal = degrees + minutes / 60 + seconds / 3600;
        if (direction === "S" || direction === "W") decimal = -decimal;

        return decimal;
    }

   // **Define Custom Icons for Each Category**
    var icons = {
        "Apada Mitra": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Apada%20Mitra.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
        "Apada Sakhi": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Apada%20Sakhi.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Fire Station": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Fire%20Station.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Fire Hydrant": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Fire%20Hydrant.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		"Ground Level Reservoir": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Ground%20Level%20Reservoir.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Overhead Reservoir": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Overhead%20Reservoir.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Open Water Source": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Open%20Water%20Source.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Mutual Aid Agency": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Mutual%20Aid%20Agency.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Major Hazardous Unit": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/Major%20Hazardous%20Unit.png", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
		 "Cyclone Shelter": L.icon({
            iconUrl: "http://localhost/wordpress/wp-content/uploads/markers/marker_biological.svg", // Replace with your icon URL
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        }),
        "Default": L.icon({
            iconUrl: "https://cdn-icons-png.flaticon.com/512/854/854878.png", // Fallback icon
            iconSize: [30, 30],
            iconAnchor: [15, 30],
            popupAnchor: [0, -30]
        })
    };


    fetch("http://localhost/wordpress/wp-content/uploads/DMRP_Items.csv")
        .then((response) => response.text())
        .then((csvText) => {
            console.log("CSV Response:", csvText);

            let rows = csvText.split("\n").slice(1);
            rows.forEach((row, index) => {
                let cols = row.split(",").map(col => col.trim());

                if (cols.length < 6) {
                    console.warn(`Skipping invalid row ${index + 1}:`, row);
                    return;
                }

                let type = cols[0];
                let category = cols[2];

                // **Fix: Merge location name if it contains commas**
                let nameParts = cols.slice(3, cols.length - 3); // Extract name parts
                let name = nameParts.join(", "); // Join them back

                let latDMS = cols[cols.length - 2].replace(/"/g, "").replace(/�/g, "°");
                let lonDMS = cols[cols.length - 1].replace(/"/g, "").replace(/�/g, "°");



                console.log(`Raw DMS for ${name}: Latitude: ${latDMS}, Longitude: ${lonDMS}`);

                let latitude = dmsToDecimal(latDMS);
                let longitude = dmsToDecimal(lonDMS);

                if (!isNaN(latitude) && !isNaN(longitude)) {
                    let markerIcon = icons[category] || icons["Default"];

                    L.marker([latitude, longitude], { icon: markerIcon })
                        .addTo(map)
                        .bindPopup(`<b>${type}</b><br>Category: ${category}<br>Name: ${name}`);
                } else {
                    console.warn(`Skipping invalid coordinates: ${latDMS}, ${lonDMS}`);
                }
            });
        })
        .catch((error) => console.error("Error loading CSV:", error));
});
